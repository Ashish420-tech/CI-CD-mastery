name: Project 41 - Enterprise Gitleaks Secret Scanning

on:
  push:
    branches:
      - project-41-gitleaks-enterprise
  pull_request:
    branches:
      - project-41-gitleaks-enterprise

permissions:
  contents: read
  pull-requests: write

jobs:
  security:
    name: Enterprise Secret Security Gate
    uses: ./.github/workflows/reusable-security.yml
