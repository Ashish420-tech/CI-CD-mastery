name: Project 42 - Trivy Filesystem Dependency Security

on:
  push:
    branches:
      - project-42-trivy-filesystem-dependency
  pull_request:
    branches:
      - project-42-trivy-filesystem-dependency

permissions:
  contents: read
  pull-requests: write

jobs:
  security:
    name: Enterprise Filesystem Security Gate
    uses: ./.github/workflows/reusable-security.yml
